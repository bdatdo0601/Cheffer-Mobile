import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    cancelButton: {
        marginRight: 8,
    },
    cancelButtonText: {
        fontSize: 16,
        fontWeight: "500",
        color: "blue",
    },
    image: { width: "100%", height: 200 },
    text: { fontSize: 19, padding: 7 },
});

export default styles;
