import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    cancelButton: {
        marginRight: 8,
    },
    cancelButtonText: {
        fontSize: 16,
        fontWeight: "500",
        color: "blue",
    },
});

export default styles;
