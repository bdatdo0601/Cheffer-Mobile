import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    flatListStyle: {
        height: "100%",
    },
    cancelButton: {
        marginRight: 8,
    },
    cancelButtonText: {
        fontSize: 16,
        fontWeight: "500",
        color: "blue",
    },
});

export default styles;
