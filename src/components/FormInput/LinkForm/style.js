import { StyleSheet } from "react-native";

export default StyleSheet.create({
    inputContainerStyle: {
        width: "100%",
    },
    containerStyle: {
        width: 300,
        bottom: 0,
        backgroundColor: "transparent",
        borderTopWidth: 0,
        borderBottomWidth: 0,
        marginBottom: 4,
    },
    input: { color: "white" }, // This is for the input text
});
